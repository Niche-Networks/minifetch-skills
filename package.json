{
  "name": "minifetch-skills",
  "version": "1.4.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/Niche-Networks/minifetch-skills"
  },
  "license": "MIT",
  "scripts": {
    "validate": "for d in skills/*/; do skills-ref validate $d || exit 1; done",
    "validate:plugin": "npx --yes -p @anthropic-ai/claude-code claude plugin validate .",
    "validate:all": "npm run validate && npm run validate:plugin"
  },
  "dependencies": {
    "skills-ref": "^0.1.5"
  }
}
